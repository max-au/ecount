{erl_opts, [debug_info]}.
{deps, []}.

{shell, [
    {apps, [ecount]}
]}.

{dialyzer, [
    {warnings, [underspecs, overspecs, unmatched_returns]},
    {get_warnings, true}
]}.

{cover_enabled, true}.
{cover_opts, [verbose]}.
